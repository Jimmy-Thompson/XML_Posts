# GigSafe Job Board - Environment Variables Configuration

# ============================================
# REQUIRED SECURITY VARIABLES
# ============================================

# Admin Session Secret - CRITICAL FOR SECURITY
# Generate a strong random secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# This should be a long, random string. NEVER commit the actual secret to version control.
ADMIN_SESSION_SECRET=your-secret-session-key-here-replace-this-with-a-long-random-string

# Admin Password - CRITICAL FOR SECURITY
# Set a strong password for accessing the admin portal
# Use a password manager to generate a secure password (at least 16 characters)
ADMIN_PASSWORD=your-strong-admin-password-here

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Port Configuration
# Default: 5000
# PORT=5000

# Node Environment - IMPORTANT FOR SECURITY
# Options: development, production
# Default: development
# Setting NODE_ENV=production enables:
#   - Secure cookies (HTTPS only)
#   - Trust proxy for accurate IP addresses behind reverse proxy
#   - Production-optimized security settings
# NODE_ENV=production

# CORS - Allowed Origins (comma-separated)
# In development, defaults to localhost
# In production, set this to your actual domain(s)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# ALLOWED_ORIGINS=https://yourdomain.com

# ============================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================

# When deploying to production:
# 1. Set NODE_ENV=production
# 2. Generate a new ADMIN_SESSION_SECRET (don't reuse development secrets)
# 3. Use a strong ADMIN_PASSWORD (minimum 16 characters)
# 4. Set ALLOWED_ORIGINS to your actual domain(s)
# 5. Ensure HTTPS is enabled (Replit handles this automatically)
# 6. Review security.log regularly for suspicious activity
# 7. Keep backup of your databases (master_jobs.db, user_jobs.db, sessions.db)

# Security Features Enabled:
# ✓ Rate limiting on all endpoints
# ✓ Brute force protection on admin login (5 attempts per 15 minutes)
# ✓ XSS sanitization on all user inputs
# ✓ CORS restrictions
# ✓ Security headers (Helmet)
# ✓ Persistent session storage
# ✓ Security event logging (logs/security.log)
# ✓ File upload validation
# ✓ Input length limits
